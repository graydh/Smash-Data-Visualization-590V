import { PostFrameUpdateType, GameStartType, GameEndType, Command, PreFrameUpdateType, ItemUpdateType, FrameBookendType } from "./slpReader";
import { Stats, FramesType, FrameEntryType } from "../stats";
export declare class SlpParser {
    private statsComputer;
    private frames;
    private settings;
    private gameEnd;
    private latestFrameIndex;
    private playerPermutations;
    private settingsComplete;
    constructor(statsComputer: Stats);
    getLatestFrameNumber(): number;
    getPlayableFrameCount(): number;
    getLatestFrame(): FrameEntryType | null;
    getSettings(): GameStartType | null;
    getGameEnd(): GameEndType | null;
    getFrames(): FramesType | null;
    handleGameEnd(payload: GameEndType): void;
    handleGameStart(payload: GameStartType): void;
    handlePostFrameUpdate(payload: PostFrameUpdateType): void;
    handleFrameUpdate(command: Command, payload: PreFrameUpdateType | PostFrameUpdateType): void;
    handleItemUpdate(command: Command, payload: ItemUpdateType): void;
    handleFrameBookend(command: Command, payload: FrameBookendType): void;
}
