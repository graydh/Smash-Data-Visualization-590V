import { FramesType, FrameEntryType, PlayerIndexedType } from "./common";
import { StatComputer } from './stats';
export interface PlayerInput {
    playerIndex: number;
    opponentIndex: number;
    inputCount: number;
}
export declare class InputComputer implements StatComputer<PlayerInput[]> {
    private playerPermutations;
    private state;
    setPlayerPermutations(playerPermutations: PlayerIndexedType[]): void;
    processFrame(frame: FrameEntryType, allFrames: FramesType): void;
    fetch(): PlayerInput[];
}
