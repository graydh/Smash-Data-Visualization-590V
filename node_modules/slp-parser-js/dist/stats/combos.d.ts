import { FrameEntryType, FramesType, ComboType, PlayerIndexedType } from "./common";
import { StatComputer } from './stats';
export declare class ComboComputer implements StatComputer<ComboType[]> {
    private playerPermutations;
    private state;
    private combos;
    setPlayerPermutations(playerPermutations: PlayerIndexedType[]): void;
    processFrame(frame: FrameEntryType, allFrames: FramesType): void;
    fetch(): ComboType[];
}
