import { FrameEntryType, FramesType, ConversionType, PlayerIndexedType } from "./common";
import { StatComputer } from './stats';
export declare class ConversionComputer implements StatComputer<ConversionType[]> {
    private playerPermutations;
    private conversions;
    private state;
    private metadata;
    constructor();
    setPlayerPermutations(playerPermutations: PlayerIndexedType[]): void;
    processFrame(frame: FrameEntryType, allFrames: FramesType): void;
    fetch(): ConversionType[];
    private _populateConversionTypes;
}
